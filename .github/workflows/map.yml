name: Update map.md

on:
  push:
    branches: ["master"]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 安装python3.9
        uses: actions/setup-python@v3
        with:
          python-version: "3.9"
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install requests
      - name: 运行map.py
        run: |
          python map.py
      - name: 提交推送
        uses: actions-js/push@master
        with:
          github_token: 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
